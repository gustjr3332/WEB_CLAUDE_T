# HACKMAN 프론트엔드 디자인 기획안 (2026-09-03)

> **반영 완료 (2026-09-03)** — 아래 방향은 `frontend/src/style.css`, `App.tsx`,
> `ContestDetail.tsx`, `index.html`에 적용되어 있다. 이 문서는 이후 화면을 추가할 때
> 따라야 할 기준으로 계속 유지한다.

리디자인 이전 `frontend/src/style.css`는 전형적인 "SaaS 카드 키트" 패턴이었다 — 모든
요소에 동일한 `border-radius: 12px` + `box-shadow: 0 1px 3px rgba(0,0,0,.08)`, 시스템
폰트, 중앙 정렬 카드 나열. 대회 운영 · 실시간 채점이라는 도메인 특성이 시각적으로 드러나지
않았다.

## 컨셉

마케팅 랜딩이 아니라 "라이브 운영 데스크". 대회 생성 → 팀 구성 → 심사위원 채점 → 실시간
스코어보드로 이어지는, 우아한형제들 해커톤(예선 15분·결선 10분)처럼 시간에 쫓기며 숫자를
계속 확인하는 도구. 스코어보드/집계 화면이 진짜 히어로여야 하며, 지금처럼 리스트 맨 아래
부록 취급되면 안 된다.

## 색상

| 토큰 | 값 | 용도 |
|---|---|---|
| Ink | `#14181C` | 본문/제목 |
| Paper | `#F3F4F1` | 배경 (크림톤 아님, 차분한 그레이-페이퍼) |
| Line | `#D8DAD4` | 헤어라인 구분선 |
| Live (진행/모집) | `#2F6F4F` | 상태 신호 — "지금 진행 중"일 때만 |
| Judging (심사중) | `#C4622A` | 긴급/주의 신호 |
| Signal Blue | `#1F3A5F` | 점수·순위 등 데이터 강조 |

크림+테라코타(`#D97757` 계열) 및 카드+그림자 반복은 의도적으로 배제 — 현재 AI 생성 UI의
가장 흔한 기본값이기 때문.

## 타이포그래피

두 서체, 역할이 명확히 구분됨:
- **Space Grotesk** — 제목/UI 텍스트 (기술적인 그로테스크, 담백함)
- **IBM Plex Mono** — 점수·날짜·순위 등 실제 숫자 데이터 전용 (탭 정렬로 스코어보드가
  전광판처럼 읽힘). 상태 라벨 등 장식 목적으로는 쓰지 않는다 — 모노스페이스를 작은 라벨에
  남용하는 것은 흔한 AI UI의 티 나는 패턴이므로 피한다.

## 레이아웃

중앙 정렬 카드 나열 → 좌측 정렬 + 얇은 헤어라인 구분의 로그/리스트형으로 전환. 상태는 둥근
배지 대신 왼쪽 컬러 바(rail) + 점(dot)으로 표시. 스코어보드 테이블의 숫자는 우측 정렬 +
모노스페이스로 실제 전광판처럼 표현.

```
대회 리스트                    스코어보드 (히어로)
┌─────────────────────┐      ┌───────────────────────────┐
│┃ 대회명A     모집중  │      │ 순위  팀        평균점수    │
│┃ 09.03–09.10  · 4팀 │      │  1    팀 알파   87.50      │
├─────────────────────┤      │  2    팀 베타   84.00      │
│┃ 대회명B     진행중  │      └───────────────────────────┘
└─────────────────────┘
```

## 원칙

1. 상태는 장식이 아니라 신호 — 컬러는 "지금 라이브"일 때만 채도를 쓴다.
2. 숫자가 곧 제품 — 스코어보드가 페이지의 무게중심이 되어야 한다.
3. 카드+그림자 대신 헤어라인+그리드로 위계를 표현한다.
4. 라이브 액센트는 하나만 — 나머지는 그레이/잉크 톤을 유지한다.

## 구현 대상 파일

- `frontend/src/style.css` — 토큰·컴포넌트 스타일 전면 교체
- `frontend/src/App.tsx` — 대회 리스트를 카드 → 헤어라인 리스트로
- `frontend/src/ContestDetail.tsx` — 스코어보드를 페이지 하단 부록에서 히어로 위치로 재배치,
  테이블 숫자 우측 정렬 + 모노스페이스 적용
- `frontend/src/AuthPanel.tsx` — 톤 맞춰 폼 스타일 조정
- Google Fonts: Space Grotesk, IBM Plex Mono 추가 필요 (`frontend/index.html` 또는
  `style.css` `@import`)
