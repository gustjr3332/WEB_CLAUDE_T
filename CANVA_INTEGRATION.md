# Canva(또는 다른 디자인 툴)로 만든 사이트에 LIKE 버튼 연동하기

지금은 `index.html`이 디자인과 좋아요 버튼 로직을 한 파일에서 같이 처리하고 있습니다.
나중에 디자인을 Canva 같은 툴로 새로 만들어서 무료 도메인에 배포하게 되면, "디자인"과
"좋아요 집계 엔진"을 분리해서 생각하는 게 가장 안전합니다.

- **디자인/레이아웃**: Canva가 담당.
- **좋아요 집계 엔진**: 지금 이 저장소(GitHub Pages)에 계속 호스팅해두고, Canva 페이지에서
  그 부분만 가져다 씀. (Apps Script 백엔드는 이미 완성되어 있고, 이번 프론트엔드를
  뭘로 만들든 전혀 안 바뀝니다.)

## 왜 Canva에 직접 코드를 옮기지 않는가

Canva Sites는 자체 요소(버튼, 텍스트 등)에 커스텀 JS 클릭 핸들러를 붙이는 걸 지원하지
않습니다. "임베드" 요소로 외부 콘텐츠를 끼워넣는 것만 안정적으로 지원됩니다. 그래서
아래 **방법 A(iframe 임베드)**가 어떤 Canva 플랜에서도 확실히 되는 방법입니다.
만약 쓰시는 플랜/앱에서 진짜 커스텀 `<script>` 삽입이 가능하다면 **방법 B**도 가능합니다.

## 방법 A — iframe으로 위젯 임베드 (추천, 항상 동작함)

이 저장소에 이미 준비해둔 `like-widget.html`은 좋아요 버튼 1개만 있는 아주 작은 페이지입니다.
URL 쿼리 파라미터로 어떤 포스트인지 알려주면 됩니다.

```
https://gustjr3332.github.io/WEB_CLAUDE_T/like-widget.html?postId=post-1&title=%EC%A0%9C%EB%AA%A9
```

- `postId`: 그 포스트를 구분하는 고유 ID (스프레드시트에 이 값으로 기록됨)
- `title`: 사람이 읽을 포스트 제목 (URL 인코딩 필요, `encodeURIComponent(title)`)

Canva 사이트 편집기에서:
1. 좋아요 버튼이 들어갈 위치에 **임베드(Embed)** 요소 추가
2. 위 형태로 만든 URL을 붙여넣기 (포스트마다 `postId`/`title`만 바꿔서 여러 번 반복)
3. 임베드 크기를 버튼 하나 들어갈 정도로 작게 조절 (배경은 투명 처리되어 있어 위젯 주변에
   Canva 디자인이 자연스럽게 비칩니다)

포스트가 5개면 임베드도 5개, 각각 다른 `postId`를 가진 위젯을 넣으면 됩니다.

### 이 위젯의 동작 방식
`script.js`와 완전히 같은 로직(같은 localStorage 키)을 씁니다:
- 클릭 → 이 브라우저에만 누적 (서버로 바로 안 보냄)
- 매일 19:00을 넘긴 첫 체크에서 **그날 누적된 모든 포스트의 좋아요를 한 번에** 배치 전송
- 전송 성공하면 0으로 초기화

같은 오리진(GitHub Pages)에서 iframe으로 여러 개 띄워도 localStorage를 공유하므로
자연스럽게 하루치가 합쳐져서 나갑니다. 탭이나 iframe이 여러 개 동시에 전송을 시도하는
경우를 대비한 잠금 로직도 들어있어서 중복 전송 걱정은 없습니다.

## 방법 B — 커스텀 코드 삽입이 가능한 경우

Canva 앱/플랜에 따라 raw HTML을 그대로 넣을 수 있는 기능이 있다면, `like-widget.html`의
`<body>` 안 내용과 `like-widget.js`/`config.js` 내용을 그대로 붙여넣어도 됩니다. 다만
`postId`/`title`을 URL 파라미터 대신 코드에 직접 하드코딩해야 합니다.

## 백엔드는 그대로입니다

Apps Script가 받는 데이터 형식은 프론트엔드가 뭐든 동일합니다:

```json
{
  "date": "2026-08-23",
  "posts": {
    "post-1": { "title": "포스트 제목", "count": 3 }
  }
}
```

즉, Canva로 완전히 새로 디자인하더라도 **이 형식으로 `APPS_SCRIPT_URL`에 POST만 보내면**
무엇으로 만든 사이트든 상관없이 똑같이 동작합니다. `config.js`의 `APPS_SCRIPT_URL`만
그대로 가리키게 해주면 됩니다. 자세한 서버 쪽 동작은 `apps-script/README.md` 참고.
